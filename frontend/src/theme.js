import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#007F00", // Color primario
    },
    secondary: {
      main: "#DBA053", // Color secundario
    },
    tertiary: {
      main: "#007CE1", // Color terciario
    },
  },
});

export default theme;
